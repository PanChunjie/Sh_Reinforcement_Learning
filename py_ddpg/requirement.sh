# requirements
# pip install pypiwin32
# pip install tensorflow==1.13.0-rc1 #this version need to add "tensorflow." before keras import
# pip install tensorflow==1.50 #this version need to remove "tensorflow." before keras import
